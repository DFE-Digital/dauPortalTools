% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-quality_helpers.R
\name{quality_get_data}
\alias{quality_get_data}
\title{Retrieve Active Quality Issue Records}
\usage{
quality_get_data(record = NULL, db_get_query = utils_db_get_query)
}
\arguments{
\item{record}{Integer scalar or \code{NULL}. Optional record ID used to filter
results. If \code{NULL} (default), all matching records are returned.}
}
\value{
A \code{\link{data.frame}} with the following columns:
\describe{
\item{Quality Concern}{Character name of the quality issue.}
\item{Description}{Character description of the issue.}
\item{Date Identified}{Datetime the issue was created.}
\item{Last Reviewed}{Datetime the issue was last checked.}
}
}
\description{
Returns unresolved quality issues for the current application, with an
optional filter for a specific record ID.
}
\details{
The current application ID is resolved using \code{\link[=utils_get_app_id]{utils_get_app_id()}}. Only
records with \code{quality_status = 0} (i.e. unresolved issues) are retrieved.

When a \code{record} value is supplied, results are further restricted to that
specific record ID.

The query joins:
\itemize{
\item \code{quality_list} (issue instances)
\item \code{quality_check} (issue definitions)
}

The database schema is resolved using \code{\link[=utils_resolve_schema]{utils_resolve_schema()}}, and the
query is executed using \code{\link[=utils_db_get_query]{utils_db_get_query()}}.

Database connections are managed internally and safely closed using
\code{on.exit()}. Logging is performed via \code{\link[=log_event]{log_event()}} at the start and
end of execution.
}
\section{Side Effects}{

\itemize{
\item Opens and closes a database connection.
\item Writes log entries via \code{\link[=log_event]{log_event()}}.
}
}

\examples{
\dontrun{
# All unresolved issues
issues <- quality_get_data()

# Filter for specific record
issues <- quality_get_data(record = 1234)
}

}
\seealso{
\code{\link[=quality_add_log]{quality_add_log()}}
}
