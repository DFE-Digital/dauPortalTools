% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-audit_helpers.R
\name{db_record_login}
\alias{db_record_login}
\title{Record a User Login Event
Inserts a record into the \code{tools_analytics} table to track when a user
accesses the application.}
\usage{
db_record_login(user = "Guest", db_execute = utils_db_execute)
}
\arguments{
\item{user}{Character scalar. Username of the user performing the login.
Defaults to \code{"Guest"}.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Record a User Login Event
Inserts a record into the \code{tools_analytics} table to track when a user
accesses the application.
}
\details{
The current application ID is resolved using \code{\link[=utils_get_app_id]{utils_get_app_id()}}. The
event is recorded with an \code{action_type} of \code{"Load"} and a fixed
\code{action_sub_type} of \code{"Initial Load"} to indicate the initial entry
point into the application.

The database schema is resolved using \code{\link[=utils_resolve_schema]{utils_resolve_schema()}}, and the
query is executed using \code{\link[=utils_db_execute]{utils_db_execute()}}.

The timestamp is generated using \code{SYSUTCDATETIME()} at the database level.

Database connections are managed internally and safely closed using
\code{on.exit()}. Logging is performed via \code{\link[=log_event]{log_event()}} at the start and
end of execution.
}
\section{Side Effects}{

\itemize{
\item Inserts a new row into the database.
\item Opens and closes a database connection.
\item Writes log entries via \code{\link[=log_event]{log_event()}}.
}
}

\examples{
\dontrun{
db_record_login(user = "BSMITH7")
}

}
\seealso{
\code{\link[=db_record_download]{db_record_download()}}
}
