% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ui-admin_helpers.R
\name{ui_role_edit_modal}
\alias{ui_role_edit_modal}
\title{User Role Edit Modal Dialog}
\usage{
ui_role_edit_modal(ns, selected)
}
\arguments{
\item{ns}{Namespace function created via \code{NS()} or \code{session$ns()} for
proper scoping of input IDs within a Shiny module.}

\item{selected}{A single-row \code{\link{data.frame}} containing user details.
Must include at least:
\itemize{
\item \code{user_id}
\item \code{username}
\item \code{role_id}
}}
}
\value{
A \code{shiny::modalDialog} UI object.
}
\description{
Generates a Shiny modal dialog that allows an administrator to update
the role assigned to a selected user.
}
\details{
Available roles are retrieved dynamically using \code{\link[=db_get_roles]{db_get_roles()}} and
presented as a selectable dropdown.

The selected user's ID is stored in a hidden input (\code{selected_user_id})
so it can be accessed by server logic when applying updates.

The modal includes:
\itemize{
\item A title showing the selected user
\item A dropdown of available roles
\item An action button (\code{apply_role_change}) to submit the update
}

This component is intended to be used with \code{\link[shiny:showModal]{showModal()}} and expects
corresponding server-side observers to handle the role update logic.
}
\section{Side Effects}{

\itemize{
\item Calls \code{\link[=db_get_roles]{db_get_roles()}} to retrieve role data
\item Generates Shiny UI elements
}
}

\seealso{
\code{\link[=db_get_roles]{db_get_roles()}}, \code{\link[shiny:modalDialog]{shiny::modalDialog()}}, \code{\link[shiny:selectInput]{shiny::selectInput()}}
}
