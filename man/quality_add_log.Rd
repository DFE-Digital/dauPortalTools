% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-quality_helpers.R
\name{quality_add_log}
\alias{quality_add_log}
\title{Insert a Quality Check Log Entry}
\usage{
quality_add_log(
  quality_check_id,
  live_issues = 0,
  new_issues = 0,
  closed_issues = 0,
  db_execute = utils_db_execute
)
}
\arguments{
\item{quality_check_id}{Integer scalar. Identifier for the quality check.}

\item{live_issues}{Integer scalar or \code{NA}. Number of currently active issues.
Defaults to \code{0}. Missing or \code{NA} values are coerced to \code{0}.}

\item{new_issues}{Integer scalar or \code{NA}. Number of newly identified issues.
Defaults to \code{0}. Missing or \code{NA} values are coerced to \code{0}.}

\item{closed_issues}{Integer scalar or \code{NA}. Number of issues closed since
the previous run. Defaults to \code{0}. Missing or \code{NA} values are coerced to \code{0}.}
}
\value{
Invisible numeric scalar indicating the number of rows affected
(typically \code{1L}).
}
\description{
Records the execution of a quality check by inserting a new row into the
\code{quality_check_log} table. The entry captures the number of live, new, and
closed issues at the time of execution.
}
\details{
All count inputs are normalised to integer scalars. Values that are \code{NULL},
length-zero, or \code{NA} are converted to \code{0}.

The database schema is resolved using \code{\link[=utils_resolve_schema]{utils_resolve_schema()}}, and the
query is executed using \code{\link[=utils_db_execute]{utils_db_execute()}}. The \code{last_run} timestamp is
recorded using \code{SYSUTCDATETIME()} at the database level.

Database connections are managed internally and safely closed using
\code{on.exit()}. Logging is performed via \code{\link[=log_event]{log_event()}} at the start and
end of execution.
}
\section{Side Effects}{

\itemize{
\item Inserts a new row into the database.
\item Opens and closes a database connection.
\item Writes log entries via \code{\link[=log_event]{log_event()}}.
}
}

\examples{
\dontrun{
quality_add_log(
  quality_check_id = 42,
  live_issues = 10,
  new_issues = 3,
  closed_issues = 2
)
}

}
\seealso{
\code{\link[=utils_db_execute]{utils_db_execute()}}, \code{\link[=log_event]{log_event()}}
}
