% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quality_add_log.R
\name{quality_add_log}
\alias{quality_add_log}
\title{Insert a New Quality Check Log Entry}
\usage{
quality_add_log(quality_check_id, new_issues, closed_issues)
}
\arguments{
\item{quality_check_id}{Integer.
The ID of the quality check being logged. Must correspond to an existing
record in the \verb{[quality_check]} table.}

\item{new_issues}{Integer.
The number of newly identified issues found during this run.}

\item{closed_issues}{Integer.
The number of issues that were closed or resolved during this run.}
}
\value{
Invisibly returns the number of rows affected (typically \code{1L})
as returned by \code{DBI::dbExecute()}.
}
\description{
Adds a new row to the \code{quality_check_log} table, recording when a quality
check was executed and how many new and closed issues were identified.
The function automatically timestamps the entry using the system time.
}
\details{
This function:
\itemize{
\item Connects to SQL Server using \code{sql_manager("dit")}.
\item Generates a timestamp in \verb{YYYY-MM-DD HH:MM:SS} format.
\item Constructs a fully parameterised SQL \code{INSERT} statement using
\code{glue::glue_sql()} for safe interpolation.
\item Inserts a new log entry into:
}

\if{html}{\out{<div class="sourceCode">}}\preformatted{\{conf$database\}.\{conf$schemas$db_schema_01a\}.quality_check_log
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
# Log a quality check run
quality_add_log(
  quality_check_id = 12,
  new_issues = 3,
  closed_issues = 1
)
}

}
\seealso{
\code{\link[DBI]{dbExecute}},
\code{\link[glue]{glue_sql}}
}
