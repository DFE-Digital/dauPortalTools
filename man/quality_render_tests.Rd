% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ui-quality_helpers.R
\name{quality_render_tests}
\alias{quality_render_tests}
\title{Render Quality Test Results Panel}
\usage{
quality_render_tests(app_id = NULL)
}
\arguments{
\item{app_id}{Integer scalar or \code{NULL}. Optional filter to restrict
results to a single application. If \code{NULL}, results for all
applications are returned.}
}
\value{
A Shiny UI object containing a GOV.UK-styled layout and an
interactive data table.
}
\description{
Generates a GOV.UK-styled Shiny UI panel displaying the most recent
quality test results for one or more applications.
}
\details{
The function:
\itemize{
\item Retrieves quality check definitions from \code{quality_check}
\item Joins application context from \code{app_list}
\item Uses \verb{OUTER APPLY} to retrieve the most recent log entry
from \code{quality_check_log} for each check
\item Filters to active checks (\code{check_active = 1})
\item Applies an optional application filter
\item Renders results as a \code{DT::datatable()}
\item Provides a download option for exporting the dataset
}

SQL is constructed using \code{\link[glue:glue_sql]{glue::glue_sql()}} to safely interpolate
parameters.

The UI is wrapped using \code{\link[shinyGovstyle:gov_layout]{shinyGovstyle::gov_layout()}}.
}
\section{Side Effects}{

\itemize{
\item Opens a database connection via \code{\link[=sql_manager]{sql_manager()}}
\item Executes SQL queries using \code{\link[DBI:dbGetQuery]{DBI::dbGetQuery()}}
\item Writes log entries via \code{\link[=log_event]{log_event()}}
}
}

\examples{
\dontrun{
quality_render_tests()

quality_render_tests(app_id = 12)
}

}
\seealso{
\code{\link[=quality_render_live]{quality_render_live()}}, \code{\link[DT:datatable]{DT::datatable()}}
}
