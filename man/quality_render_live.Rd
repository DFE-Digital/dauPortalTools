% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ui-quality_helpers.R
\name{quality_render_live}
\alias{quality_render_live}
\title{Render Live Quality Issue Panel}
\usage{
quality_render_live(user = NULL, with_rcs = NULL, region = NULL, app_id = NULL)
}
\arguments{
\item{user}{Character scalar or \code{NULL}. Optional filter for a specific user.}

\item{with_rcs}{Logical or \code{NULL}. Optional filter indicating whether the
record is associated with RCS.}

\item{region}{Character scalar or \code{NULL}. Optional filter for region.}

\item{app_id}{Integer or \code{NULL}. Optional filter restricting results to a
single application.}
}
\value{
A Shiny UI object. The returned object includes an attribute
\code{"data"} containing the underlying \code{data.frame} used to populate the table.
}
\description{
Generates a GOV.UK-styled UI panel displaying active quality issues for one or
more applications. Results are rendered as an interactive table with optional
filtering and a download option.
}
\details{
The function:
\itemize{
\item Queries the \code{quality_list} table for active issues (\code{check_active = 1})
\item Applies optional filters for application, user, region, and RCS status
\item Joins application and quality metadata from \code{app_list} and
\code{quality_check}
\item Builds contextual links for records where applicable
\item Renders results using \code{\link[DT:datatable]{DT::datatable()}}
\item Provides a download option for exporting the dataset
}

SQL queries are constructed using \code{\link[glue:glue_sql]{glue::glue_sql()}} to ensure safe
interpolation of input parameters.

The UI is wrapped using \code{\link[shinyGovstyle:gov_layout]{shinyGovstyle::gov_layout()}}.
}
\section{Side Effects}{

\itemize{
\item Opens a database connection via \code{\link[=sql_manager]{sql_manager()}}
\item Executes SQL queries using \code{\link[DBI:dbGetQuery]{DBI::dbGetQuery()}}
\item Writes log entries via \code{\link[=log_event]{log_event()}}
}
}

\examples{
\dontrun{
quality_render_live()

quality_render_live(app_id = 3)

quality_render_live(user = "BSMITH7", region = "North West")
}

}
\seealso{
\code{\link[=quality_get_data]{quality_get_data()}}, \code{\link[=db_add_portal_message]{db_add_portal_message()}}
}
