% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-admin_helpers.R
\name{db_get_app_users}
\alias{db_get_app_users}
\title{Retrieve Application Users and Assigned Roles#' Retrieve'
Returns all users along with their assigned role for the current
application. Users without an assigned role are returned with a
default role of \code{"User"}.}
\usage{
db_get_app_users()
}
\value{
A \code{\link{data.frame}} with the following columns:
\describe{
\item{user_id}{Integer user identifier.}
\item{username}{Character username.}
\item{email}{Character email address.}
\item{role_name}{Character role name (defaults to \code{"User"} if none assigned).}
\item{role_id}{Integer role identifier (may be \code{NA}).}
}
}
\description{
Retrieve Application Users and Assigned Roles#' Retrieve'
Returns all users along with their assigned role for the current
application. Users without an assigned role are returned with a
default role of \code{"User"}.
}
\details{
The current application ID is resolved using \code{\link[=utils_get_app_id]{utils_get_app_id()}}.
The database schema is resolved via \code{\link[=utils_resolve_schema]{utils_resolve_schema()}}, and
the query is executed using \code{\link[=utils_db_get_query]{utils_db_get_query()}}.

The query performs a left join between:
\itemize{
\item \code{users}
\item \code{user_roles} (filtered to the current application)
\item \code{roles}
}

Results are ordered alphabetically by username.

Database connections are managed internally and safely closed using
\code{on.exit()}. Logging is performed via \code{\link[=log_event]{log_event()}} at the start
and end of execution.
}
\section{Side Effects}{

\itemize{
\item Opens and closes a database connection.
\item Writes log entries via \code{\link[=log_event]{log_event()}}.
}
}

\examples{
\dontrun{
users <- db_get_app_users()

# View users
head(users)
}

}
\seealso{
\code{\link[=db_get_roles]{db_get_roles()}}, \code{\link[=db_update_user_role]{db_update_user_role()}}
}
