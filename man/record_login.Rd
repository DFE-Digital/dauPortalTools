% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/record_login.R
\name{record_login}
\alias{record_login}
\title{Record User Login Activity}
\usage{
record_login(user = "Guest")
}
\arguments{
\item{user}{Character. The username to record. Defaults to \code{"Guest"}.}
}
\value{
No return value. The function is called for its side effect of recording the login.
}
\description{
This function logs a user's visit to the application by inserting a record into the
\code{tools_analytics} table. It captures details such as the timestamp, page name, action type,
username, and associated application ID.
}
\details{
The function reads configuration details from \code{config.yml} to determine the application ID
and database schema. It then constructs and executes an SQL \code{INSERT} statement to log the
visit. Logging messages are generated at the start and end of the process.
}
\examples{
\dontrun{
# Record a login for the default user
record_login()

# Record a login for a specific user
record_login(user = "ben.smith")
}

}
